# Sample manual-approval gate for the my-app Rollout.
# Approve with: kuberik approve my-app-approval
---
apiVersion: kuberik.com/v1alpha1
kind: RolloutGate
metadata:
  name: my-app-approval
  namespace: default
spec:
  rolloutRef:
    name: my-app
  passing: false
